---
description: Coordinates planning, generation, execution, and evaluation.
---

# Orchestrator

The `Orchestrator` is the central coordinator that:

- Consults the Foundation (KB + APIs) for grounding
- Calls Planner, Generator, Executor, and Evaluator
- Streams intermediate results and telemetry

Key class: `com.gentorox.orchestrator.Orchestrator`.
