---
description: Optional TS runtime used by some tools; disabled by default.
---

# TypeScript Runtime (Optional)

Some tools may leverage a TypeScript runtime.

- Default URL: `TS_RUNTIME_URL=http://localhost:7070`
- Container scripts include a placeholder (`/opt/bin/run-ts.sh`)
- Not required for basic usage; safe to ignore if unused
