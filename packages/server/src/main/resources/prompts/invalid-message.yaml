activations:
  - role: user
    activation: retry
    content: |
      The provided message does not comply with the expected format.
      
      We agreed that the reply should follow the format below:
      
      {{ typeTemplate | raw }}
      
      ---
      
      While attempting to parse the YAML, the following error was reported:
      
      ```
      
      {{ errors | raw }}
      
      ```
