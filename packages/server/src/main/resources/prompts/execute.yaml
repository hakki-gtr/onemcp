activations:
  - role: user
    activation: execution-summary
    content: |
      Snippet was executed successfully.
      
      Here is the complete list of added / changed values:
      
      {% for value in variables %}
      
      - **`{{ value.identifier | raw }}`**
      
        ```md
      
      {{ ident( value.description, 4) | raw }}
      
      
        ```
      
      ## Model
      
        Here is the JSON Schema for this entry, so you can understand how to interact with it:   
        
        ```json
          
      {{ ident(value.model, 4) | raw }}
          
        ```

      ---
      {% endfor %}
